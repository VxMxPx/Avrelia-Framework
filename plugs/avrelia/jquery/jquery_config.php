<?php if (!defined('AVRELIA')) { die('Access is denied!'); }

$jquery_config = array
(
    # Should we load it from local source?
    'local' => false,

    # Which version do we need?
    'version' => '1.7.1',
);
